--- a/index.js	1970-01-01 01:00:01.000000000 +0100
+++ b/index.js	2025-08-26 19:17:42.777632028 +0200
@@ -71,7 +72,8 @@
 
   traverse(toMsgContent(message), function (obj, rel) {
     if (opts.rel && rel !== opts.rel) return
-
+    
+    if (obj == null) return
     var r = (typeof obj == 'string') ? obj : obj.link
     if (any) {
       if (!ref.isLink(r)) return
@@ -154,4 +156,4 @@
     rels.push(rel)
   })
   return rels
-}
\ No newline at end of file
+}
